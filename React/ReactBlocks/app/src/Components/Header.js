import React, {Component} from "react";
import styles from './Color.module.css';

class Header extends Component{
    render() {
        return (
            <div className= {styles.head}>Header
            </div>
        );
    }
}

export default Header;